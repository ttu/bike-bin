# Bike Bin — Development Guide

> **Purpose:** How to run, debug, and work on the project locally.  
> **Source of truth:** `package.json`, `app.json` / `app.config.*`, README in repo root.

---

_To be filled as the project is implemented. Include: prerequisites, clone & install, env vars (Supabase URL/key), run dev server / Expo, run on device/simulator, debugging, common issues, and useful scripts._
